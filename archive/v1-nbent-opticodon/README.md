# v1: NBent_OptiCodon

This archive entry records the original predecessor project:

https://github.com/HybridizedBlott/NBent_OptiCodon

The v1 codebase is not vendored into FactorForge and is not installed with the
current package. It is listed here only to document the historical lineage from
the early *Nicotiana benthamiana* codon-optimization prototype to FactorForge.

Historical tokenizer comparison scripts and skipped v1/v2 comparison tests are
kept in this archive entry for context only.

Use the current production engine for new work:

```python
from factorforge.engines.profile import RuleBasedOptimizer
```
