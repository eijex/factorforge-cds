# Vercel Serverless Function Requirements
# Minimal dependencies for FactorForge v3.x

biopython>=1.81
numpy>=2.2.6
pandas>=2.0.0

# Optional: ViennaRNA (if using MFE scoring)
# Note: ViennaRNA may not work on Vercel due to system dependencies
# Comment out if deployment fails
# viennarna>=2.6.0
