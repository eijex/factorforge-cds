[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "factorforge"
version = "2.5.3"
description = "FactorForge — Codon optimization engine by Eijex. Engineering Intrinsic Expression."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
authors = [{ name = "Eijex", email = "hello@eijex.com" }]
keywords = ["codon optimization", "synthetic biology", "protein expression", "machine learning", "ESM2", "BART"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
]

dependencies = [
    "biopython>=1.81",
    "requests>=2.31",
]

[project.optional-dependencies]
ml = [
    "torch>=2.0",
    "transformers>=4.35",
    "fair-esm>=2.0",
    "pyyaml>=6.0",
]
dev = [
    "pytest>=7.0",
    "ruff>=0.1",
]

[project.urls]
Homepage = "https://eijex.com"
Repository = "https://github.com/eijex/factorforge"
Issues = "https://github.com/eijex/factorforge/issues"

[project.scripts]
factorforge = "factorforge.cli.main:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py310"
